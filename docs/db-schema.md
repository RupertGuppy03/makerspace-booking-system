# Database schema

> TODO — not written yet.

What belongs here:

- Each table: columns, types, nullability, defaults.
- Relationships (a reservation belongs to one tool and one user).
- Any constraints that enforce rules — e.g. no two reservations overlapping on the same tool.
- Notes on Supabase row-level security, if we use it.

The actual SQL lives in `database/`. This file explains it in English.
