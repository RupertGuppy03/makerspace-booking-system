# API

> TODO — not written yet.

What belongs here: one section per endpoint, in a consistent format.

```
GET /api/tools
Auth: required
Returns: Tool[]
```

Keep this in sync with the controllers in `backend/src/ToolBooking.Api/Controllers/`. If you change
an endpoint, change this file in the same PR — the frontend team reads this, not your C#.
